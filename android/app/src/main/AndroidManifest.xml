<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Phase G: required for geolocator's real GPS fix in
         fulfillment_provider.dart's useCurrentLocation(). Coarse is
         requested as a fallback if the user denies fine location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
 
    <!-- FIX: previously only declared in the debug/profile manifests
         (android/app/src/{debug,profile}/AndroidManifest.xml), which
         Flutter auto-injects for dev builds only. Without it here in
         `main`, a release build silently loses all network access —
         Supabase, Google Maps tiles, and the new geocoding reverse
         lookup would all fail on a real release APK/Play build. -->
    <uses-permission android:name="android.permission.INTERNET"/>
 
    <application
        android:label="crave"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- PLAN.md Phase 0 already calls for generating a Maps API key
             restricted to this app's SHA-1 + package name. Paste that
             real key here — this placeholder will not compile the Maps
             SDK correctly and "Use current location" not working. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDon_mp5UvBOfuDPg2izkE9LIGdIsmqX90"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.
 
         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
 